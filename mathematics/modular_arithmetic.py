if __name__ == "__main__":
    a = 73
    b = 2
    print(f"mod : {a % b}")

# Laws of modular arithmetic
# (a+b)%m = (a%m + b%m) % m
# remainder of sum = (sum of remainders) % m
# (a-b)%m = if l = a%m - b%m is negative l+m or l
# (a*b)%m = (a%m * b%m)%m
